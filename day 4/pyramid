rows = 5

for i in range(1, rows + 1):
    # Print fewer leading spaces with each new row.
    for space in range(rows - i):
        print(" ", end="")

    # Print an odd number of stars: 1, 3, 5, 7, 9.
    for star in range(2 * i - 1):
        print("*", end="")

    print()